{
  "name": "login-cypress",
  "version": "1.0.0",
  "description": "Site estático com formulário de cadastro de pet + Cypress E2E (didático)",
  "scripts": {
    "start": "http-server -p 8080 -c-1 .",
    "test": "cypress run",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.13.0",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "vercel": "^32.0.1"
  }
}
